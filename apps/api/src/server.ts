/**
 * Application wiring starts in the authentication milestone.
 * Keeping this entry point intentionally small prevents route handlers from
 * becoming the place where authorization, Prisma access, and realtime logic mix.
 */
export {};
